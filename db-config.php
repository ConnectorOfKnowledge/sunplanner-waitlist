<?php
/**
 * Database credentials for the SunPlanner waitlist.
 *
 * Fill these in after creating the MySQL database in Hostinger hPanel:
 *   hPanel > Hosting > Manage > Databases > MySQL Databases
 *   1. Create a database (e.g. u123456_sunplanner)
 *   2. Create a database user with a strong password
 *   3. Assign the user to the database (All Privileges)
 *   4. Copy the values into the constants below.
 *
 * The host on Hostinger shared hosting is almost always "localhost".
 */

$DB_HOST = 'localhost';
$DB_NAME = 'FILL_IN_DB_NAME';    // e.g. u123456_sunplanner
$DB_USER = 'FILL_IN_DB_USER';    // e.g. u123456_spuser
$DB_PASS = 'FILL_IN_DB_PASS';    // the password you set for that user
